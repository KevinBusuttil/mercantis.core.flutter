name: mercantis_core_mono
description: Mercantis Core Flutter monorepo root.
publish_to: none

environment:
  sdk: '>=3.3.0 <4.0.0'

# Root pubspec required by Melos to anchor the workspace.
# Packages live under packages/ and app/.
